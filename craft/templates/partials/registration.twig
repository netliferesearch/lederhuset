<nav id="registration" class="login registration">

  <div class="grid-wrap pb--xlarge">
    <button class="login__close close" id="registrationClose">
      <div class="close__text-wrapper">
        <span class="close__text borders--bottom paragraph">Lukk</span>
      </div>
      <div class="close__icon-wrapper">
        <span class="close__icon"></span>
      </div>
    </button>
  </div>

  <div class="grid-wrap registration__outer">
    <div class="registration__wrapper login__wrapper pb--xlarge">
      <h2 class="center pb--small">Registrer deg</h2>
      <div class="registration__inner input">
        <form method="post" accept-charset="UTF-8">
            {{ getCsrfInput() }}
            <input type="hidden" name="{{ craft.config.csrfTokenName }}" value="{{ craft.request.csrfToken }}">
            <input type="hidden" name="action" value="users/saveUser">
            <input type="hidden" name="redirect" value="/">

            {% macro errorList(errors) %}
                {% if errors %}
                    <ul class="errors">
                        {% for error in errors %}
                            <li>{{ error }}</li>
                        {% endfor %}
                    </ul>
                {% endif %}
            {% endmacro %}

            {% from _self import errorList %}

            <div class="input__wrapper mb">
              <input class="login__input input__input" id="regFirstName" type="text" name="firstName"
                  {%- if account is defined %} value="{{ account.firstName }}"{% endif -%}>
              <span class="login__placeholder input__placeholder paragraph no-flicker">Fornavn</span>
              {% if account is defined %}
                  {{ errorList(account.getErrors('firstName')) }}
              {% endif %}
            </div>

            <div class="input__wrapper mb">
              <input class="login__input input__input" id="regLastName" type="text" name="lastName"
                  {%- if account is defined %} value="{{ account.lastName }}"{% endif -%}>
              <span class="login__placeholder input__placeholder paragraph no-flicker">Etternavn</span>
              {% if account is defined %}
                  {{ errorList(account.getErrors('lastName')) }}
              {% endif %}
            </div>

            <div class="input__wrapper mb">
              <input class="login__input input__input" id="regUserPhone" type="tel" name="userPhone"
                  {%- if account is defined %} value="{{ account.userPhone }}"{% endif -%}>
              <span class="login__placeholder input__placeholder paragraph no-flicker">Tlf./Mobil</span>
              {% if account is defined %}
                  {{ errorList(account.getErrors('userPhone')) }}
              {% endif %}
            </div>

            <div class="input__wrapper mb">
              <input class="login__input input__input" id="regUserCompanyName" type="text" name="userCompanyName"
                  {%- if account is defined %} value="{{ account.userCompanyName }}"{% endif -%}>
              <span class="login__placeholder input__placeholder paragraph no-flicker">Virksomhet</span>
              {% if account is defined %}
                  {{ errorList(account.getErrors('userCompanyName')) }}
              {% endif %}
            </div>

            <div class="input__wrapper mb">
              <input class="login__input input__input" id="regUserOrganizationNumber" type="text" name="userOrganizationNumber"
                  {%- if account is defined %} value="{{ account.userOrganizationNumber }}"{% endif -%}>
              <span class="login__placeholder input__placeholder paragraph no-flicker">Organisasjonsnummer</span>
              {% if account is defined %}
                  {{ errorList(account.getErrors('userOrganizationNumber')) }}
              {% endif %}
            </div>

            <div class="input__wrapper mb">
              <input class="login__input input__input" id="regEmail" type="email" name="email"
                  {%- if account is defined %} value="{{ account.username }}"{% endif -%}>
              <span class="login__placeholder input__placeholder paragraph no-flicker">E-post (brukernavn)</span>
              {% if account is defined %}
                  {{ errorList(account.getErrors('email')) }}
              {% endif %}
            </div>

            <div class="input__wrapper mb">
              <input class="login__input input__input" id="reg-password" type="password" name="password">
              <span class="login__placeholder input__placeholder paragraph no-flicker">Passord</span>
              {% if account is defined %}
                  {{ errorList(account.getErrors('password')) }}
              {% endif %}
            </div>

            <button class="login__submit input__submit lh-button lh-button--icon paragraph" type="submit">
              <span class="lh-button__text">Bli abonnement</span>
              <svg class="lh-button__arrow" width="12" height="8" viewBox="0 0 177 118">
                <path d="M117.3,47V0l58.9,58.6l-58.9,59.2V70.3H0.3V47H117.3z"/>
              </svg>
            </button>
        </form>
      </div>

		</div>
  </div>
</nav>
