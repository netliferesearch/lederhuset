{% if not craft.config.devMode %}
{% requireLogin %}
{% endif %}
<!doctype html>

<head>
	<meta charset="utf-8" />
  <meta name="viewport" content="width=device-width" />
  <link rel="shortcut icon" href="/favicon.ico" />

  <title>{% if title is defined %}{{ title }} - {% endif %}{{ siteName }}</title>

  <link rel="home" href="{{ siteUrl }}" />
  <link rel="stylesheet" href="/webpack-dist/screen.css">
</head>

<body class="t-smooth">
  {% if craft.Adminbar is defined and entry is defined and not craft.request.isLivePreview %}
    {{ craft.Adminbar.show(entry) }}
  {% endif %}

  {% cache if not craft.config.devMode %}
    {% include 'partials/header' %}
  {% endcache %}

  <main id="top">
	    {% block content %}
	    {% endblock content %}
  </main>

		{% include 'partials/footer' %}

  {% includeJsFile "/webpack-dist/bundle.js" %}
</body>
